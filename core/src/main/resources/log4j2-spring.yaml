Configuration:
  name: Default

  Appenders:
    Console:
      name: Console
      target: SYSTEM_OUT
      PatternLayout:
        pattern: '%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight{[%-5level]} [%t] %c{1} - %msg%n'

  Loggers:
    Root:
      level: info
      AppenderRef:
        - ref: Console
