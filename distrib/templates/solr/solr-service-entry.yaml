apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: solr
  namespace: {{ namespace }}
spec:
  hosts:
    - {{ solr_host }}
  ports:
    - number: {{ solr_port }}
      name: http
      protocol: HTTP
  resolution: DNS
  location: MESH_EXTERNAL