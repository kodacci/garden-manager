apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: solr-rule
  namespace: {{ namespace }}
spec:
  exportTo:
    - "."
    - istio-system
  hosts:
    - {{ solr_host }}
  gateways:
    - mesh
    - {{ namespace }}/garden-manger-egress-gateway
  http:
    - match:
        gateways:
          - mesh
        port: {{ solr_port }}
      route:
        - destination:
            host: istio-egressgateway.istio-system.svc.cluster.local
            subset: solr
            port:
              number: 80
    - match:
        - gateways:
            - {{ namespace }}/garden-manager-egress-gateway
          port: {{ solr_port }}
      route:
        - destination:
            host: {{ solr_host }}
            port:
              number: {{ solr_port }}